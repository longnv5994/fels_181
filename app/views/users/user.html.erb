<li>
  <%= gravatar_for user, size = Settings.avatar.size %>
  <%= link_to User.name, user %>
  <% if logged_in? and current_user.is_admin? && !current_user?(user) %>
    <%= link_to t "delete", user, method: :delete,
      data: {confirm: t "sure"} %>
  <% end %>
</li>
