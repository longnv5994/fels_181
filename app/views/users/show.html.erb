<% provide :title, @user.name %>
<div class="row">
  <div class="col-md-4">
    <section class="user_info">
      <h2>
        <%= gravatar_for @user %>
        <%= @user.name %>
        <p><%= link_to t("edit_profile"), edit_user_path, class: "btn" %></p>
      </h2>
    </section>
  </div>
</div>
